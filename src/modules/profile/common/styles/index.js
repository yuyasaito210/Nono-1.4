import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  boxContainer: {
    borderRadius: 20,
    padding: 20,
    position: 'relative',
    overflow: 'hidden'
  }  
})
